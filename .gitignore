node_modules/
dist/
.turbo/
*.tsbuildinfo

# env — never commit secrets
.env
.env.dev
.env.local
.env.*.local

# logs / os
*.log
.DS_Store
__pycache__/
*.pyc

# Cross-repo secrets guard — real console secrets live only in landed-infra; never commit them from anywhere.
console/secrets/
